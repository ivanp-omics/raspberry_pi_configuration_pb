# Razvoj: runtime + alati. Na Pi-ju se NE instalira - ondje ide samo
# requirements.txt, jer pytest/ruff/httpx na uredaju nemaju sto raditi.
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.5
